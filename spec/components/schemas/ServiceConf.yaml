type: object
properties:
  name:
    type: string
    description: 配置名称
    pattern: '[a-z0-9A-Z-_\.]+'
  content:
    type: string
    description: 服务配置模板内容
  updated:
    type: string
    description: 更新时间
  revision:
    type: string
    description: 配置模板版本
  type:
    type: string
    enum: ['spec', 'conf']