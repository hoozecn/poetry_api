type: object
properties:
  os:
    type: string
    description: 运行os，如 ubuntu:14.04, ubuntu:16.04
  cap:
    type: string
    enum: ['high', 'medium', 'low']
  buildTools:
    type: array
    items:
      $ref: "#/components/schemas/BuildTool"