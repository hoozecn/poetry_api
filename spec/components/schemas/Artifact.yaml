type: object
properties:
  id:
    type: integer
    description: 应用包ID
  name:
    type: string
    description: 名称
  created:
    type: string
    description: 生成日期
  link:
    type: string
    description: 应用包地址
  type:
    type: string
    enum: ['image', 'object']
    description: image为镜像，object为压缩文件
